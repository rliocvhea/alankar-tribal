{
  "name": "ecommerce-fullstack",
  "version": "1.0.0",
  "description": "Full-stack e-commerce application with React and Express",
  "scripts": {
    "install:server": "cd server && npm install",
    "install:client": "cd client && npm install",
    "build:client": "cd client && npm run build",
    "start": "cd server && node server.js",
    "railway:build": "npm run install:server && npm run install:client && npm run build:client"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
